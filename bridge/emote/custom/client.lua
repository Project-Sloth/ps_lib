-- Play an emote
function ps.playEmote(emote, variant)
    return lib.print.debug('ps.playEmote not integrated for custom emote resource')
end

-- Cancel an emote
function ps.cancelEmote(skipReset)
    return lib.print.debug('ps.cancelEmote not integrated for custom emote resource')
end