import {
	containsForbiddenContent,
	containsForbiddenNameChars,
	sanitizeText,
} from "./security";

export const security = {
	containsForbiddenContent,
	containsForbiddenNameChars,
	sanitizeText,
};
